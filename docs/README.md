# Scandium Labs — MVP scaffold

This workspace contains an initial scaffold for the Scandium Labs MVP (backend FastAPI skeleton, DB helper, requirements, and docker-compose). Follow the build guide in `scandium_labs_mvp_build_guide.md` to continue.

Next steps:
- Implement `structure_parser` service and `ml/graph_builder.py`
- Add Celery worker tasks and model checkpoint loading
- Scaffold the frontend with Vite + React
